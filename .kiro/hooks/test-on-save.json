{
  "name": "Test on Save",
  "description": "Run tests when formula files are saved",
  "trigger": {
    "type": "on-save",
    "filePattern": "src/lib/**/*.ts"
  },
  "action": {
    "type": "command",
    "command": "npm run test:run"
  },
  "enabled": true
}
